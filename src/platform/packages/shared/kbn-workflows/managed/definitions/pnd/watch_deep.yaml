version: "1"
name: __WATCH_NAME__
description: __WATCH_DESCRIPTION__
enabled: true
tags:
__WATCH_TAGS__
triggers:
  - type: manual
steps:
  - name: watch_policy
    type: data.set
    with:
      watch:
        mandate: Deep investigation & hunts
        autonomyLevel: 3
        handoff: records
        onDemand: true
        draft: false
        cadence: manual
        from: 8
        to: 18
        mode: window
        ui:
          color: "#8b5cf6"
          icon: console
          order: 40
        scopeSummary: Security indices · EDR · DNS
        scopes:
          - name: Security indices
            access: full
            label: Read
          - name: EDR telemetry
            access: full
            label: Read
          - name: DNS · netflow
            access: full
            label: Read
        __WATCH_CALLABLES__
  - name: specialist_stub
    type: console
    with:
      message: "Deep Watch skeleton — add ai.agent / skill steps (see Floor)"
